新需求：
1. 更工程化和组件化一点，把菜单底部导航栏做成可读取配置的，因为要考虑后续还要加菜单和功能，当前激活菜单做成全局状态
2. 样式优化，底部菜单固定，页面内容根据情况滚动，现在测试发现切换页面时会出现多个滚动条
3. github的配置要做成隐藏的，特定操作下才能打开，比如长按同时点击时跳转到配置页，然后配置项再代码做一个默认的配置，也可以手动再修改
4. 再增加一个长页面，兼容可以在微信公众号或者小程序中打开，这个页面就不要菜单这些了，直接读取所有的照片，然后做滚动慢慢加载，后续还可能会加一下特效

再次优化：
1. 点击样式优化，更适配移动端，检查一下其他页面，现在测试发现在页面内容不多的情况下也会滚动，应该是min-height: 100% 导致的
2. github 的配置单独做成一个路由页面，还是只有特定情况下才能访问，再生成一下默认配置

测试后反馈：
1. 页面的滚动还是有点问题，现在内容超过也不会滚动了
2. 上传的照片要增加一个删除的入口
3. 另发现现在都不是使用的路由，改成文件系统路由，页面都用路由来跳转

测试反馈：
1. 上一个反馈的第一个滚动的问题没有处理，第二个删除也没有加。
2. 文件系统路由可以使用 pages 插件实现。